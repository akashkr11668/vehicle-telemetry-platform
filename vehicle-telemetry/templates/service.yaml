apiVersion: v1
kind: Service
metadata:
  name: {{ include "vehicle-telemetry.fullname" . }}
  labels:
    {{- include "vehicle-telemetry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vehicle-telemetry.selectorLabels" . | nindent 4 }}
