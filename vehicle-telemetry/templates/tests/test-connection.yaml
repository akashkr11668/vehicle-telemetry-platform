apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vehicle-telemetry.fullname" . }}-test-connection"
  labels:
    {{- include "vehicle-telemetry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vehicle-telemetry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
