services:

  backend:
    build: .
    container_name: telemetry_backend
    ports:
      - "5000:5000"

    volumes:
      - ./logs:/app/logs
  
  simulator:
    build: .
    container_name: telemetry_simulator

    environment:
      - BACKEND_URL=http://backend:5000/telemetry
      
    command: python3 simulator/vehicle_simulator.py
    depends_on:
      - backend